@model IEnumerable<yazlab3.Models.Order> 
<h1>Sipariş Durumu</h1>

<table class="table">
    <thead>
        <tr>
            <th>Ürün Adı</th>
            <th>Adet</th>
            <th>Toplam Fiyat</th>
            <th>Sipariş Tarihi</th>
            <th>Durum</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.Product.ProductName</td>
                <td>@order.Quantity</td>
                <td>@order.TotalPrice TL</td>
                <td>@order.OrderDate.ToString("dd.MM.yyyy HH:mm")</td>
                <td>@order.OrderStatus</td>
                @* <td>
                    @if (order.OrderStatus == "Onay Bekliyor")
                    {
                        <form asp-action="Buy" asp-controller="Customers" method="post">
                            <input type="hidden" name="OrderID" value="@order.OrderID" />
                            <button type="submit" class="btn btn-success">Siparişi Onayla</button>
                        </form>
                    }
                </td> *@
            </tr>
        }
    </tbody>
</table>
